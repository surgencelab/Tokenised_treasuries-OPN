# Dashboard environment (copy to dashboard/.env.local and fill in)

# Same Postgres database as the pipeline. Use a pooled endpoint if you have
# one; add ?sslmode=require if your Postgres needs TLS.
TREASURIES_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/tokenized_treasuries
